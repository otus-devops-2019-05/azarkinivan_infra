---
- name: install ruby and bundle
  hosts: all
  become: true
  tasks:
    - name: Install packages with_items
      apt: "name={{ item }} state=present"
      with_items:
        - ruby-full
        - ruby-bundler
        - build-essential
